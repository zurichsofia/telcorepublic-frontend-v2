/**
 * DOM ids for programmatic post-hero scroll alignment (no CSS `scroll-snap-type` on
 * `<html>` — see `SnowMountainHero` and `ServiceVideoHeroV2` `scrollend` handlers).
 */
export const HOME_WHY_SNAP_ID = "home-why-snap";

/** First full-viewport block under `#service-detail` on `/services/[slug]`. */
export const SERVICE_DETAIL_INTRO_SNAP_ID = "service-detail-intro-snap";
